Section Header

  + name      := PARSER_CLI;

Section Inherit

  + parent_parser :Expanded PARSER;

Section Private

  + cluster :CLUSTER;

Section CLUSTER

  - make c:CLUSTER :SELF <-
  (
    cluster := c;
    Self
  );
  
  - parse <-
  (
    open_source (cluster.filename);
  );
