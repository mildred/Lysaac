Section Header

  + name := TYPE_FINDER;

Section Public

  - find_prototype type_name:CSTRING :MAYBE(PROTOTYPE) <- (deferred; NULL);
  
  - find_type type_name:CSTRING :MAYBE(TYPE) <- (deferred; NULL);


