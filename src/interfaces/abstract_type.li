Section Header

  + name := ABSTRACT_TYPE;

Section Inherit

  + parent_any :Expanded ANY;
  
Section Public

  - is_pointer                :BOOLEAN       <- (deferred; FALSE);
  - dereference_abstract_type :ABSTRACT_TYPE <- (deferred; NULL);
  
  - generate gen:GENERATOR <- deferred;

