Section Header

  + name := INSTR_STORE;

Section Inherit

  + parent_instr :Expanded INSTR;

Section Public

  //
  // Link
  //
  
  - link gen:GENERATOR;

  //
  // Generate
  //

  - generate gen:GENERATOR <-
  (
    gen.store value to pointer;
  );
  
  - make_store_in ptr:REG value v:REG :SELF <-
  [
    -? { ptr.base_type.pointer };
  ]
  (
    v.base_type.resolve_with_and_assert (ptr.base_type.pointer_type);
    pointer := ptr;
    value   := v;
    made := TRUE;
    Self
  );

Section Private

  + pointer    :REG;
  + value      :REG;

